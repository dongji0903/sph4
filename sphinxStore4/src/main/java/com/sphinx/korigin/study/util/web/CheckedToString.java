package com.sphinx.korigin.study.util.web;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.sphinx.korigin.study.util.domain.Utill;

public class CheckedToString {

	/*Utill util = new Utill();

	@Override
	public String execute(HttpServletRequest request,
			HttpServletResponse response) {

		request.setAttribute("checkedList",
				util.arrayToString(request.getParameterValues("check")));
		return getNextPage();

	}
*/
}
