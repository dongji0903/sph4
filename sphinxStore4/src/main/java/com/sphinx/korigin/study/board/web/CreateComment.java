package com.sphinx.korigin.study.board.web;

import java.util.Date;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;


public class CreateComment {

	/*@Override
	public String execute(HttpServletRequest request,HttpServletResponse response) {
		
		Comment cvo = new Comment();
		cvo.setRef(Integer.parseInt(request.getParameter("num")));
		cvo.setNickname(request.getParameter("name"));
		cvo.setContent(request.getParameter("content"));
		cvo.setCategory(request.getParameter("category"));
		Date d = new Date();
		cvo.setWritedate(d);
		
		BoardDao bDao = BoardDaoImpl.getInstance();
		bDao.insertComments(cvo);
		
		return getNextPage();
	}
*/
}
