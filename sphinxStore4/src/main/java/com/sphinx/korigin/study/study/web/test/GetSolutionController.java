package com.sphinx.korigin.study.study.web.test;

import java.util.List;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;


public class GetSolutionController {
	/*AnswerSheetService as=new AnswerSheetServiceImpl();
	TestSheetService ts=new TestSheetServiceImpl();
	@Override
	public String execute(HttpServletRequest request,
			HttpServletResponse response) {
		AnswerSheet asheet=(AnswerSheet) request.getAttribute("asheet");
		String sid=asheet.getSid();
		List<Solution> solutionList=as.findSolutionsOfSheet(sid);
		List<Question> questionList=ts.findQuestionsForSolution(sid);
		request.setAttribute("soList", solutionList);
		request.setAttribute("qList", questionList);
		return getNextPage();
	}*/

}
