package com.sphinx.korigin.study.study.web.exam;


import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;


public class GetExamSheetController {

	/*TestSheetService ts = new TestSheetServiceImpl();
	
	@Override
	public String execute(HttpServletRequest request,
			HttpServletResponse response) {
	ExamSheet est = (ExamSheet) ts.findSheet(request.getParameter("sid"));
	System.out.println(est);
	request.getSession().setAttribute("sheet", est);
		return getNextPage();
	}
*/
}
