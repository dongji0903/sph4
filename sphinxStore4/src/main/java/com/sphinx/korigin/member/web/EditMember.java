package com.sphinx.korigin.member.web;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;



public class EditMember {
/*
	MemberDao md = MemberDaojdbcImpl.getInstance();
	
	@Override
	public String execute(HttpServletRequest request,
			HttpServletResponse response) {
		
		Member member = md.findMember(request.getParameter("userId"));
		
		member.setPassword(request.getParameter("userPass"));
		member.setEmail(request.getParameter("userEmail"));
		member.setAddress(request.getParameter("userAddr"));
		
		md.modifyMember(member);
		
		return getNextPage();
	}*/

}
